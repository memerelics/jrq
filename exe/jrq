#!/usr/bin/env ruby

require 'jrq'

require 'json'
require 'hashie'

_ = JSON.load(STDIN.read)
_ = Hashie::Mash.new(_)

if ARGV.length.zero?
  puts JSON.pretty_generate(_)
else
  puts JSON.pretty_generate(eval(ARGV.first))
end
